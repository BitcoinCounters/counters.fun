# npm 10's peer resolver crashes on vitest 4's peer graph
# ("Cannot read properties of null (reading 'edgesOut')") when building a tree
# from scratch. Pinning the version does not help — the cycle is inside
# vitest's own optional peers. This flag is the documented workaround and the
# only reason a fresh clone installs at all.
legacy-peer-deps=true
